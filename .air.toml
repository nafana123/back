root = "."
tmp_dir = "/tmp/air"  # Временные сборки в tmp, не вмонтированные в проект

[build]
cmd = "go build -o /tmp/air/main ./cmd/api"
bin = "/tmp/air/main"
include_ext = ["go"]
exclude_dir = ["vendor"]
delay = 500

[log]
time = true